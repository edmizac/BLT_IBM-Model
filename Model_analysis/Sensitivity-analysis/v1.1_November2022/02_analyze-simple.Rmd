---
title: "02_analysis-simple"
author: "Eduardo Zanette"
date: "2022-11-21"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# packages
library("here")
library("nlrx")
library("ggplot2")
library("dplyr")
library("readr")


```

### Read empirical tree files
```{r}
pathfiles <- here("Data", "Movement", "Resource-trees")
trees_to_dfs <- list.files(pathfiles, pattern = ".csv")
unwanted <- list.files(pathfiles, pattern = "all.csv")
trees_to_dfs <- base::setdiff(trees_to_dfs, unwanted) #https://stackoverflow.com/questions/31590730/list-files-in-r-that-do-not-match-a-pattern

# i <- 20
# for (tf in trees_to_dfs) {
  
  # tf <- trees_to_dfs[i]
  # tf_csv <- paste0(pathfiles, "/", tf)
  # csv <- read_csv(tf_csv)
  
  tf_csv <- paste0(pathfiles, "/", trees_to_dfs)
  
  trees <- trees_to_dfs %>%
    purrr::map_df(~read_csv(paste0(pathfiles, "/", .), col_types = cols(.default = "?", id_day_all = "c"))) %>%
    mutate(species = case_when(
      
    ))
    dplyr::select(-c("id_day_all", "sp"))
  
  # if (i == 1) {
  #   csv1 <- readr::read_csv(tf_csv)
  # }
  # 
  # trees <- dplyr::bind_rows(trees, csv1)
  
  # i <- i + 1
# }

```

